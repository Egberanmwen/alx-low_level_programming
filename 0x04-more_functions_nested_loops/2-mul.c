#include "main.h"
/**
*mul-a function that multiplies two integers.
*@a:first input.
*@b:second input.
*Return:result of the multipliaction of the two inputs.
*/
int mul(int a, int b)
{
return (a * b);
}
