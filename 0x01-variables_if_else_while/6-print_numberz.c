#include <stdio.h>
/**
 * main -Entry point
 * betty style doc for function main is inserted here
 * Return: Always 0 (Success)
 */
int main(void)
{

	int b;

/*Your code is inserted here*/
while (b <= 10)
{putchar (b + 0), b++;
}
putchar('\n');
return (0);
}
